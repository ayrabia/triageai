# PostgreSQL connection string (RDS in production, local Postgres in dev)
DATABASE_URL=postgresql://user:password@host:5432/triageai?sslmode=require

# AWS Cognito
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
NEXT_PUBLIC_COGNITO_REGION=us-east-1
NEXT_PUBLIC_COGNITO_APP_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# AWS S3 bucket for referral PDFs
S3_BUCKET=triageai-test-referrals
AWS_REGION=us-east-1

# Secret for pipeline → ingest callback authentication
PIPELINE_SECRET=your-secret-here
